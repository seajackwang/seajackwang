package laowang
/**
 * 逻辑方法 与&& 或||
 * 按位与运算（&），按位或运算（|）和按位异或运算（^）
 */
object scala_yunsuanfu {
  def main(args: Array[String]): Unit = {
    val list: List[Int] = List(1, 2, 3, 4, 5, 6, 7, 8)
    for ( i<-0 until list.length ){
      val a = list(i)
      //或||
//      if (a % 2 == 0 || a == 5){
//        println(a)
//      }


    }
  }
}
